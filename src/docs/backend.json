{
  "entities": {
    "Participant": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Participant",
      "type": "object",
      "description": "Represents a participant in the congress.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the Participant (Firebase Auth UID)."
        },
        "name": {
          "type": "string",
          "description": "Full name of the participant."
        },
        "email": {
          "type": "string",
          "description": "Email address of the participant.",
          "format": "email"
        },
        "avatarUrl": {
          "type": "string",
          "description": "URL of the participant's profile picture."
        },
        "digitalCredentialQR": {
          "type": "string",
          "description": "Data for the QR code, which is the user's UID."
        },
        "points": {
          "type": "number",
          "description": "Total gamification points earned by the participant."
        }
      },
      "required": [
        "id",
        "name",
        "email",
        "digitalCredentialQR"
      ]
    }
  },
  "auth": {
    "providers": [
      "password"
    ]
  },
  "firestore": {
    "structure": [
      {
        "path": "users/{userId}",
        "definition": {
          "entityName": "Participant",
          "schema": {
            "$ref": "#/backend/entities/Participant"
          },
          "description": "Collection to store participant profile data. User ID is the Firebase Auth UID.",
          "params": [
            {
              "name": "userId",
              "description": "The Firebase Auth UID of the participant."
            }
          ]
        }
      }
    ]
  }
}
